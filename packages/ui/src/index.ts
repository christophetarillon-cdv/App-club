// Platform-agnostic UI tokens only — no JSX here
// Web components → apps/web/src/components
// Mobile components → apps/mobile/src/components
// Shared design tokens usable in both Tailwind and NativeWind

export * from './tokens';
