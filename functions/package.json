{
  "name": "@cdv/functions",
  "version": "0.0.1",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "npx esbuild src/index.ts --bundle --platform=node --target=node20 --format=cjs --external:firebase-admin --external:firebase-functions --external:nodemailer --external:fluent-ffmpeg --external:ffmpeg-static --external:pdf-lib --external:googleapis --outfile=lib/index.js",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.0.0",
    "fluent-ffmpeg": "^2.1.3",
    "googleapis": "^173.0.0",
    "nodemailer": "^6.9.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@types/fluent-ffmpeg": "^2.1.28",
    "@types/nodemailer": "^6.4.0",
    "typescript": "^5.4.0"
  }
}
